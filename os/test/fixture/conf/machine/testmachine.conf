# testmachine — fixture machine.conf, sourced by the engine (load_config). os/test builds no real
# artifacts, so no BSP facts are needed; this exists to prove conf/machine/$MACHINE.conf gets sourced.
TEST_MACHINE=testmachine
