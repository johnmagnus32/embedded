# Build trees (toolchain, downloaded tarballs, all outputs) — reproducible from the recipes/sources +
# pinned versions, so none of it belongs in git.
/image/build/    # the OS image build (the engine's PRODUCT_DIR workspace)
/build/          # the host console-app build (src/Makefile -> build/sim, used by run-sim.sh)
