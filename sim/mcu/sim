#!/bin/bash
# sim — Run the game in the browser (no debugger)
# sim-web.py spawns sim-core internally, so we just launch sim-web.
set -e
DIR="$(cd "$(dirname "$0")" && pwd)"
ELF="${1:?Usage: $0 <firmware.elf>}"

exec python3 "$DIR/src/sim-web/sim-web.py" --machine gameboy --firmware "$ELF"
